# 03.frontend-02.visual - 视觉风格规范

> **文档版本**：v1.0.0
> **创建日期**：2026-01-13
> **基于**：ZERO 框架规范

---

## 📍 文档定位

本文档定义轻养到家项目的**视觉设计风格标准**。

---

## 🎯 核心风格

### 设计关键词

**优雅、大气、现代、有呼吸感**

### 风格解读

| 关键词 | 含义 | 实现方式 |
|--------|------|----------|
| 优雅 | 精致细腻，不粗糙 | 细节打磨、微妙过渡、精选字体 |
| 大气 | 留白充足，不拥挤 | 大间距、大字号标题、宽松布局 |
| 现代 | 简洁时尚，不过时 | 扁平设计、几何图形、渐变点缀 |
| 呼吸感 | 视觉舒适，不压抑 | 充足留白、柔和配色、层次分明 |

---

## 📐 间距规范

### 呼吸感间距

```scss
// 区块间距（section 之间）
$section-gap: 120px;      // PC
$section-gap-mobile: 80px; // 移动端

// 内容内边距
$section-padding: 100px 0;      // PC
$section-padding-mobile: 60px 0; // 移动端

// 容器最大宽度
$container-max-width: 1200px;
$container-padding: 0 40px;      // PC
$container-padding-mobile: 0 20px; // 移动端
```

### 元素间距

| 场景 | PC | 移动端 |
|------|-----|--------|
| 标题与副标题 | 16px | 12px |
| 标题与内容 | 40px | 24px |
| 卡片间距 | 32px | 20px |
| 列表项间距 | 24px | 16px |

---

## 🎨 配色规范

### 品牌色

```scss
$primary-color: #07c160;      // 微信绿 - 主色
$primary-light: #e8f8ef;      // 浅绿背景
$primary-dark: #059c4e;       // 深绿强调
```

### 中性色

```scss
$text-primary: #1a1a1a;       // 主文字
$text-secondary: #666666;     // 次要文字
$text-tertiary: #999999;      // 辅助文字
$border-color: #e5e5e5;       // 边框
$background-light: #f8f9fa;   // 浅灰背景
$background-white: #ffffff;   // 白色背景
```

### 渐变色

```scss
// 主渐变（用于 Hero 区域）
$gradient-primary: linear-gradient(135deg, #07c160 0%, #10b981 100%);

// 浅色渐变（用于背景）
$gradient-light: linear-gradient(180deg, #f8f9fa 0%, #ffffff 100%);
```

---

## 📝 字体规范

### 字体家族

```scss
$font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'PingFang SC', 
              'Hiragino Sans GB', 'Microsoft YaHei', sans-serif;
```

### 字号层级

| 用途 | PC | 移动端 | 字重 |
|------|-----|--------|------|
| 大标题 H1 | 56px | 36px | 700 |
| 区块标题 H2 | 40px | 28px | 600 |
| 小标题 H3 | 24px | 20px | 600 |
| 正文 | 16px | 15px | 400 |
| 辅助文字 | 14px | 13px | 400 |

### 行高

```scss
$line-height-tight: 1.3;    // 标题
$line-height-normal: 1.6;   // 正文
$line-height-loose: 1.8;    // 长文本
```

---

## 🖼️ 布局模式

### Hero 区域

```scss
.hero {
  min-height: 100vh;           // 全屏高度
  display: flex;
  align-items: center;
  justify-content: center;
  text-align: center;
  padding: 120px 40px;
  
  @media (max-width: 768px) {
    min-height: auto;
    padding: 100px 20px;
  }
}
```

### 内容区块

```scss
.section {
  padding: 100px 0;
  
  @media (max-width: 768px) {
    padding: 60px 0;
  }
}

.section-header {
  text-align: center;
  margin-bottom: 60px;
  
  @media (max-width: 768px) {
    margin-bottom: 40px;
  }
}
```

### 交替背景

```scss
// 奇偶区块交替背景，增加层次感
.section:nth-child(odd) {
  background: $background-white;
}

.section:nth-child(even) {
  background: $background-light;
}
```

---

## ✨ 动效规范

### 过渡时间

```scss
$transition-fast: 0.2s ease;
$transition-normal: 0.3s ease;
$transition-slow: 0.5s ease;
```

### 滚动动画

```scss
// 元素进入视口时的动画
.fade-in-up {
  opacity: 0;
  transform: translateY(30px);
  transition: opacity 0.6s ease, transform 0.6s ease;
  
  &.visible {
    opacity: 1;
    transform: translateY(0);
  }
}
```

### 悬停效果

```scss
// 卡片悬停
.card {
  transition: transform 0.3s ease, box-shadow 0.3s ease;
  
  &:hover {
    transform: translateY(-8px);
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
  }
}

// 按钮悬停
.button {
  transition: all 0.3s ease;
  
  &:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 20px rgba(7, 193, 96, 0.3);
  }
}
```

---

## 🃏 卡片规范

### 基础卡片

```scss
.card {
  background: #fff;
  border-radius: 16px;
  padding: 40px;
  box-shadow: 0 4px 20px rgba(0, 0, 0, 0.05);
  
  @media (max-width: 768px) {
    padding: 24px;
    border-radius: 12px;
  }
}
```

### 特色卡片

```scss
.feature-card {
  background: #fff;
  border-radius: 20px;
  padding: 48px 40px;
  text-align: center;
  box-shadow: 0 8px 30px rgba(0, 0, 0, 0.06);
  
  .icon {
    width: 80px;
    height: 80px;
    margin: 0 auto 24px;
    background: $primary-light;
    border-radius: 20px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: $primary-color;
    font-size: 36px;
  }
}
```

---

## 🔘 按钮规范

### 主按钮

```scss
.button-primary {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  height: 52px;
  padding: 0 40px;
  font-size: 16px;
  font-weight: 500;
  color: #fff;
  background: $primary-color;
  border: none;
  border-radius: 26px;
  cursor: pointer;
  transition: all 0.3s ease;
  
  &:hover {
    background: $primary-dark;
    transform: translateY(-2px);
    box-shadow: 0 8px 20px rgba(7, 193, 96, 0.3);
  }
}
```

### 次按钮

```scss
.button-secondary {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  height: 52px;
  padding: 0 40px;
  font-size: 16px;
  font-weight: 500;
  color: $primary-color;
  background: transparent;
  border: 2px solid $primary-color;
  border-radius: 26px;
  cursor: pointer;
  transition: all 0.3s ease;
  
  &:hover {
    background: $primary-color;
    color: #fff;
  }
}
```

---

## ❌ 禁止事项

1. **禁止拥挤布局** - 元素间必须有足够间距
2. **禁止纯黑文字** - 使用 #1a1a1a 代替 #000000
3. **禁止生硬边角** - 卡片圆角不小于 12px
4. **禁止无过渡动画** - 交互元素必须有过渡效果
5. **禁止过小字号** - 正文不小于 14px

---

**最后更新**：2026-01-13
