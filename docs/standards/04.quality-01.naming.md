# 04.quality-01.naming - 命名规范

> **文档版本**：v1.0.0
> **创建日期**：2026-01-12
> **基于**：ZERO 框架规范

---

## 📍 文档定位

本文档定义轻养到家项目的**命名哲学和规范**，是所有命名的指导原则。

---

## 🎯 核心哲学

### 完整描述 > 缩写简写

```
❌ cmd, cfg, usr, btn, msg, req, res
✅ command, config, user, button, message, request, response

❌ auth.cmd.php
✅ authentication.api.php

❌ usr_cfg_lvl
✅ user_config_level
```

### AI 友好 > 人类打字方便

```
❌ getUserInfo()
✅ getUserProfileInformation()

❌ handleErr()
✅ handleErrorResponse()
```

### 行业通用 > 项目特定

```
❌ 项目特定缩写：.cmd.php, .lab.php
✅ 行业通用命名：.api.php, .include.php
```

---

## 🚫 禁止使用的缩写

| 禁止 | 应该用 |
|------|--------|
| `cmd` | `command` |
| `cfg` | `config` |
| `usr` | `user` |
| `btn` | `button` |
| `msg` | `message` |
| `req` | `request` |
| `res` | `response` |
| `err` | `error` |
| `cb` | `callback` |
| `fn` | `function` |
| `obj` | `object` |
| `arr` | `array` |
| `str` | `string` |
| `num` | `number` |
| `idx` | `index` |
| `len` | `length` |
| `val` | `value` |
| `tmp` | `temporary` |

### 例外：行业公认缩写

| 允许 | 原因 |
|------|------|
| `id` | 行业标准 |
| `url` | 行业标准 |
| `api` | 行业标准 |
| `html` | 行业标准 |
| `css` | 行业标准 |
| `json` | 行业标准 |
| `sql` | 行业标准 |
| `http` | 行业标准 |
| `ip` | 行业标准 |

---

## 📋 命名长度指南

| 类型 | 推荐长度 | 举例 |
|------|---------|------|
| 变量名 | 10-30 字符 | `userProfileInformation` |
| 函数名 | 15-40 字符 | `validateUserEmailAddress()` |
| 文件名 | 15-50 字符 | `UserProfileSettings.vue` |
| 数据库表 | 20-50 字符 | `user_profile_settings` |
| 数据库字段 | 15-40 字符 | `profile_email_verified` |

---

## 🔄 文件后缀规范

### PHP 文件

| 后缀 | 用途 | 举例 |
|------|------|------|
| `.api.php` | API 入口文件 | `list.api.php` |
| `.include.php` | 包含文件 | `database.include.php` |
| `.config.php` | 配置文件 | `app.config.php` |

### 前端文件

| 后缀 | 用途 | 举例 |
|------|------|------|
| `.vue` | Vue 组件 | `Home.vue` |
| `.js` | JavaScript | `main.js` |
| `.scss` | 样式文件 | `main.scss` |

---

## 🗄️ 数据库命名规范

### 表命名

```
✅ user_order_list
✅ service_category
✅ technician_schedule

❌ tbl_user
❌ t_order
❌ UserOrder
```

### 字段命名

```
✅ created_at
✅ updated_at
✅ is_deleted
✅ order_status

❌ createTime
❌ UpdatedAt
❌ isDeleted
```

### 字段前缀规范

| 前缀 | 用途 | 举例 |
|------|------|------|
| `is_` | 布尔值 | `is_active`, `is_deleted` |
| `has_` | 布尔值 | `has_paid`, `has_reviewed` |
| `can_` | 布尔值 | `can_cancel`, `can_refund` |
| `_at` | 时间戳 | `created_at`, `updated_at` |
| `_count` | 计数 | `order_count`, `view_count` |

---

## 💡 AI 开发友好性检查

### 命名自检问题

1. **AI 能否仅通过名称理解用途？**
   - ❌ `processData()` - 处理什么数据？
   - ✅ `processUserOrderData()` - 清晰

2. **是否使用了行业通用术语？**
   - ❌ `qy_usr_cfg` - 需要查阅项目文档
   - ✅ `user_config` - 自解释

3. **新开发者能否立即理解？**
   - ❌ `handleCb()` - 需要上下文
   - ✅ `handleApiResponseCallback()` - 自解释

---

## ✅ 检查清单

- [ ] 没有使用禁止的缩写
- [ ] 文件后缀使用行业通用命名
- [ ] 变量/函数名完整描述其用途
- [ ] 数据库表/字段名语义清晰
- [ ] AI 能仅通过名称理解代码意图

---

**最后更新**：2026-01-12
