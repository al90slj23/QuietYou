# 01.arch-02.structure - 项目结构规范

> **文档版本**：v1.0.0
> **创建日期**：2026-01-12
> **基于**：ZERO 框架规范

---

## 📍 文档定位

本文档定义轻养到家项目的**目录结构和路径映射规则**。

---

## 📁 项目目录结构

```
QuietYou/
├── api/                    # 后端 API
│   ├── admin/              # 管理后台 API
│   ├── merchant/           # 商户端 API
│   ├── tech/               # 技师端 API
│   ├── user/               # 用户端 API
│   ├── authentication.include.php
│   ├── bootstrap.include.php
│   ├── config.include.php
│   ├── database.include.php
│   └── response.include.php
│
├── apps/                   # 前端 Vue 应用（源码）
│   ├── home/               # 官网首页
│   └── user/               # 用户端
│
├── packages/               # 共享代码包
│   └── shared/
│       └── common/         # 通用样式和 API
│
├── pages/                  # 前端页面（构建产物 + 静态页面）
│   ├── admin/              # 管理后台
│   ├── home/               # 官网首页（Vue 构建产物）
│   ├── merchant/           # 商户端
│   ├── tech/               # 技师端
│   └── user/               # 用户端（Vue 构建产物）
│
├── sql/                    # 数据库脚本
│   └── schema.sql
│
├── docs/                   # 项目文档
│   └── standards/          # 开发规范
│
├── tests/                  # 测试文件
│
├── go.sh                   # 统一入口脚本
├── go.lib.sh               # 脚本库
├── go.0.sh                 # 本地开发
├── go.1.sh                 # GitHub + 服务器部署
├── go.2.sh                 # 仅 rsync 同步
├── go.3.sh                 # 运行测试
├── go.4.sh                 # 数据库操作
│
├── .env                    # 环境变量（不提交）
├── .env.example            # 环境变量示例
├── .deployignore           # 部署排除规则
└── package.json            # 项目配置
```

---

## 🔄 路径映射规则

### 映射转换方法

| 源 | 目标 | 转换方法 | 举例 |
|---|------|---------|------|
| 文件夹路径 | 数据库表 | 下划线连接 | `admin_users_list` |
| 文件夹路径 | API路径 | 加 `/api/` 前缀 | `/api/admin/users/list` |
| 文件夹路径 | 权限标识 | 点号连接 | `admin.users.list` |

### 完整举例

```
路径：pages/admin/order/list/
  → 表：admin_order_list
  → API：/api/admin/order/list
  → 权限：admin.order.list

路径：pages/user/service/detail/
  → 表：user_service_detail
  → API：/api/user/service/detail
  → 权限：user.service.detail
```

---

## 🌐 URL 路由规则

### 访问地址映射

| URL | 实际路径 | 说明 |
|-----|---------|------|
| `https://qy.im.sh.cn/` | `/pages/home/` | 官网首页 |
| `https://qy.im.sh.cn/admin/` | `/pages/admin/` | 管理后台 |
| `https://qy.im.sh.cn/merchant/` | `/pages/merchant/` | 商户端 |
| `https://qy.im.sh.cn/tech/` | `/pages/tech/` | 技师端 |
| `https://qy.im.sh.cn/user/` | `/pages/user/` | 用户端 |

### Nginx 配置位置

```
/www/server/panel/vhost/nginx/extension/qy.im.sh.cn/url_rewrite.conf
```

---

## 📦 前端应用结构

### Vue 应用（apps/）

```
apps/home/
├── index.html
├── package.json
├── vite.config.js
├── public/
└── src/
    ├── App.vue
    ├── main.js
    ├── components/
    ├── views/
    └── styles/
```

### 构建输出

- `apps/home/` → 构建到 `pages/home/`
- `apps/user/` → 构建到 `pages/user/`

---

**最后更新**：2026-01-12
